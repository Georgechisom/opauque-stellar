# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Large ZK artifacts — fetch via npm run fetch:circuits (see artifacts/README.md)
public/circuits/

# Playwright run output (reports, traces, failure screenshots) — regenerated
# per run, not committed. Baseline screenshots live in
# e2e/tests/*.spec.ts-snapshots/ instead and ARE committed.
test-results/
playwright-report/
blob-report/
playwright/.cache/
